// import NotificationSounds, { playSampleSound, stopSampleSound } from 'react-native-notification-sounds';

export const playNotificationSounds = async() => {
    // const sound = await NotificationSounds.getNotifications('notification');
    // if (sound.length > 0) {
    //     playSampleSound(sound[0]);
    // }
    return null;
};