import React, { useState, useRef } from 'react';
import { View, Modal,Text } from 'react-native';
import Styled from 'styled-components';

const PopUpCouponSucces = ({ navigation, route }) => { 

    return (
        <Modal>

        </Modal>
    )
}

export default PopUpCouponSucces;